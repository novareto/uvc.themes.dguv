<div id="site-action"> 
  <ul>
    <tal:actions repeat="action component.viewlets">
      <li tal:attributes="class repeat['action'].end and len(component.viewlets) > 1 and 'last' or ''">
	<a tal:replace="structure action.render()" />
      </li>
    </tal:actions>
  </ul>
</div>
